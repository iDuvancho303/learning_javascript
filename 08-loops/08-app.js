// Loops

// Using "For" loops

 for (var i=0; i<=15; i++)  {

// Initializer or starter point
// condition for when your loop should end
// incrementor or decrementor -- continiues up or down
 }

 console.log(i);

 var word = "abcd";

 function doubleChar(str) {
    
    for (str < 0; str < 4; str++)
      return word = word[i] + word[i];
    }

    console.log(word);